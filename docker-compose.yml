version: '3.8'

services:
  postgres:
    image: postgres:16.3
    container_name: postgres
    environment:
      POSTGRES_USER: bitcoindeepa-dca
      POSTGRES_PASSWORD: bitcoindeepa-dca
      POSTGRES_DB: bitcoindeepa-dca
    ports:
      - '3009:5432'
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
